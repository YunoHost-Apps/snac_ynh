Your snac2 instance is now running.

The admin user **__ADMIN__** was created with password: `__GENERATED_PASSWORD__`

Log in at: `https://__DOMAIN____PATH__/__ADMIN__/admin`

You can reset the password later with:

```
sudo -u __ID__ __INSTALL_DIR__/snac resetpwd __DATA_DIR__ __ADMIN__
```

All configuration options are available in the YunoHost admin panel under **Apps > snac2 > Config Panel**.
